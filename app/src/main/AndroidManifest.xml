<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.nullvoid.blooddonation">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LoginUser" android:label="Login"/>
        <activity android:name=".SignupUser" android:label="Sign Up"/>
        <activity android:name=".DonorRegistration" android:label="Donate Blood"/>
        <activity android:name=".DoneeFrom" android:label="Request Blood" />
        <activity android:name=".DoneeList" android:label="Donees" />

    </application>

</manifest>